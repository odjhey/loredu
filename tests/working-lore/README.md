# working-lore — M3, the bounded context packet

Catalog rows **T41–T45** exercise the published application API: exact global budgets
and compact disclosure as history/Scope grows; retracted and superseded knowledge
omitted but reachable through anchored disclosure; conflicts and revalidation in
full-count attention sections; every handle resolving through show/history; and
Working Lore Basis replay/staleness after one append. Compiled CLI T40 separately
owns the five rendered zero-count lines and exact empty-success exit behavior.

The property under test is *boundedness with reachability* — a packet that grows
with the store has failed even when every assertion about its content passes.
Row **T75** is application-owned here: every globally truncated section states its
full count and carries a permutation-digest/exact-occurrence-bound continuation.
The compiled CLI suite adds unannotated reinforcement without reclaiming the row.
The shared-cursor parity suite obtains genuine cursors for all five application read
operations, proves every foreign endpoint pair, independently validates both duplicated
query copies, and rejects malformed transport/schema plus impossible terminal lore
resumes before callbacks or partial output.

Contract: [working lore](../../docs/architecture/contracts/working-lore.md).
